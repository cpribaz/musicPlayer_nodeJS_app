<h2>Add Tracks</h2>
<label>Enter Track Name</label>
<form action="/playlists/<%= playlist.id %>/addTracks?_method=PUT" method="POST">
    <div class="form-row">
        <div class="form-item">
            <label>Enter Track Name</label>
            <input type="text" name="tracks">
        </div>
    </div>
    <div class="form-row ">
        <button class="btn btn-primary" type="submit">Add Track</button>
    </div>
</form>